return {
    -- 背景色的红色值，范围为[0.0, 1.0]
    background_color_r = 0.1,
    -- 背景色的绿色值，范围为[0.0, 1.0]
    background_color_g = 0.1,
    -- 背景色的蓝色值，范围为[0.0, 1.0]
    background_color_b = 0.1,
    -- 背景色的透明值，范围为[0.0, 1.0]
    background_color_a = 1.0,

    -- 图片容器在横屏时位于右方的宽度
    container_width = 200,
    -- 图片容器在竖屏时位于下方的高度
    container_height = 200,
    -- 图片容器布局的间隔
    container_layout_spacing = 20,
    -- 图片容器布局的顶部间距
    container_layout_padding_top = 20,
    -- 图片容器布局的底部间距
    container_layout_padding_bottom = 20,
    -- 图片容器布局的左部间距
    container_layout_padding_left = 20,
    -- 图片容器布局的右部间距
    container_layout_padding_right = 20,
    -- 图片容器背景色的红色值，范围为[0.0, 1.0]
    container_background_color_r = 0.1,
    -- 图片容器背景色的绿色值，范围为[0.0, 1.0]
    container_background_color_g = 0.1,
    -- 图片容器背景色的蓝色值，范围为[0.0, 1.0]
    container_background_color_b = 0.1,
    -- 图片容器背景色的透明值，范围为[0.0, 1.0]
    container_background_color_a = 0.9,

    -- 节点被选中的边框尺寸
    cell_selected_border_size = 6,
    -- 节点被选中的红色值，范围为[0.0, 1.0]
    cell_selected_color_r = 0.7,
    -- 节点被选中的绿色值，范围为[0.0, 1.0]
    cell_selected_color_g = 0.1,
    -- 节点被选中的蓝色值，范围为[0.0, 1.0]
    cell_selected_color_b = 0.1,
    -- 节点被选中的透明值，范围为[0.0, 1.0]
    cell_selected_color_a = 0.9,

    -- 加载进度的字体大小
    progress_font_size = 28,
}
