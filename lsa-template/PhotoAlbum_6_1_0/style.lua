return {
    -- 背景色的红色值，范围为[0.0, 1.0]
    background_color_r = 0.13,
    -- 背景色的绿色值，范围为[0.0, 1.0]
    background_color_g = 0.13,
    -- 背景色的蓝色值，范围为[0.0, 1.0]
    background_color_b = 0.13,
    -- 背景色的透明值，范围为[0.0, 1.0]
    background_color_a = 1.0,

    -- 相片容器在横屏时位于右方的宽度
    container_width = 400,
    -- 相片容器在竖屏时位于下方的高度
    container_height = 400,
    -- 相片节点的行数或列数
    cell_roworcolumn= 2,
    -- 相片节点的间隔
    cell_spacing = 20,
    -- 相片节点与边框的间距
    cell_margin = 20,
    -- 节点被选中的边框尺寸
    cell_selected_border_size = 6,
    -- 节点被选中的红色值，范围为[0.0, 1.0]
    cell_selected_color_r = 1.0,
    -- 节点被选中的绿色值，范围为[0.0, 1.0]
    cell_selected_color_g = 1.0,
    -- 节点被选中的蓝色值，范围为[0.0, 1.0]
    cell_selected_color_b = 1.0,
    -- 节点被选中的透明值，范围为[0.0, 1.0]
    cell_selected_color_a = 0.8,

    -- 加载进度的字体大小
    progress_font_size = 28,

    -- 最大放大倍数
    viewer_zoomin_max = 4,

    -- 工具栏x轴边距
    toolbar_margin_x = 40,
    -- 工具栏x轴边距
    toolbar_margin_y = 26,
    -- 工具栏宽度
    toolbar_width = 36,
    -- 工具栏高度
    toolbar_height = 260,
    -- 缩放条间距
    toolbar_slider_margin = 100,
    -- 描述开关按钮尺寸
    toolbar_toogle_size = 64,

    -- 描述框的背景色的红色值，范围为[0.0, 1.0]
    description_background_color_r = 0.0,
    -- 描述框的背景色的绿色值，范围为[0.0, 1.0]
    description_background_color_g = 0.0,
    -- 描述框的背景色的蓝色值，范围为[0.0, 1.0]
    description_background_color_b = 0.0,
    -- 描述框的背景色的透明值，范围为[0.0, 1.0]
    description_background_color_a = 0.6,
    
    -- 描述框的边距
    description_margin_x = 24,
    -- 描述框的边距
    description_margin_y = 24,
    -- 描述框的文字大小
    description_font_size = 22,
    -- 描述框的文字空白填充
    description_font_padding = 22,

}
